<%= render :partial => 'forem/posts/head', :locals => { :topic => @topic } %>

<%= simple_form_for [forem, @topic.forum, @topic, @post] do |f| %>
  <%= f.submit t("forem.post.buttons.edit") %>
  <div class="clear"></div><br>
  <%= render :partial => "form", :locals => { :f => f } %>
  <%= f.submit t("forem.post.buttons.edit") %>
<% end %>
