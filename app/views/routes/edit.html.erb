<div class="form">
  <fieldset>
    <legend>Enter in the 
    <% if @trip %>
	Trip Report and
     <% end %>
     <%= @type %> Data</legend>
     <%= render 'form' %>
  </fieldset>
</div>

<%= link_to 'Show', @route %> |
<%= link_to 'Back', routes_path %>
